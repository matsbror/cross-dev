version: '3'
services:
  riscv64:
    image: riscv64
    privileged: true
    container_name: riscv64_container
    volumes:
      - ./shared:/home/ace5g/shared
    command: tail -f /dev/null  # Keeps the container running in the foreground

